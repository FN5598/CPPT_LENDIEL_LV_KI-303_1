# Звіт до лабораторної роботи № 1

## Тема і варіант

- Предметна область: лікарські засоби.
- Номер варіанта: 11.
- Операційна система: macOS, Apple Silicon (arm64).

## Реалізація

Програма розміщена в `src/main/java/ua/lpnu/lendiel/vitalii/lab01/`.
Вхідні дані зберігаються у `src/main/resources/lab01/Data.csv`.

Програма читає CSV-рядки з classpath, перевіряє кількість і вміст полів, визначає коректні та некоректні записи, а також обчислює середню ціну, кількість рецептурних препаратів, найкоротший термін придатності й загальну кількість коректних рядків.

Помилки валідації зберігаються у файлі `Errors.txt` і виводяться в консоль.

## Збірка і запуск

```bash
./mvnw clean test
./mvnw package
java -cp target/classes ua.lpnu.lendiel.vitalii.lab01.Lab01Application
```

## GitHub Issues і Pull Request

- Issues: [Issue #3](https://github.com/FN5598/CPPT_LENDIEL_LV_KI-303_1/issues/3), [Issue #1](https://github.com/FN5598/CPPT_LENDIEL_LV_KI-303_1/issues/1).
- Pull Requests: [Pull Request #4](https://github.com/FN5598/CPPT_LENDIEL_LV_KI-303_1/pull/4), [Pull Request #2](https://github.com/FN5598/CPPT_LENDIEL_LV_KI-303_1/pull/2).
- Відповідність вимогам: реалізовано читання CSV з classpath, перевірку кількості та значень полів, обробку помилок, розрахунок статистики й запис помилок у `Errors.txt`.

## Тестування, CI і артефакти

- Команда тестування: `./mvnw clean test`.
- Результат: компіляція успішна; виконано 2 тести, 1 тест завершився помилкою в `Lab01ApplicationTest.loadsCsvDataFromResources`, оскільки тест очікує заголовок `Name;Form;5.2;2;false`, а поточний `Data.csv` починається з `Indian;Pills;5.2;2;false`.
- Перевірка пакування: `./mvnw -DskipTests package` завершилася успішно.
- Запуск програми: середня ціна — `3.64`, кількість рецептурних препаратів — `3`, найкоротший термін — `1`, коректних рядків — `5`, виявлено `1` помилку.
- CI / GitHub Actions: [перегляд запуску CI](https://github.com/FN5598/CPPT_LENDIEL_LV_KI-303_1/actions/runs/35134222874).
- JAR-артефакти: JAR-файли для всіх трьох операційних систем доступні внизу сторінки відповідного запуску GitHub Actions.

## Академічна доброчесність

Цей текст підготовлено з використанням Codex GPT-5.6 Luna.

**Ваш внесок:** застосунок написано власноруч. Самостійно перевірено структуру програми, логіку обробки CSV, результати запуску та відповідність вимогам лабораторної роботи.

**Внесок ШІ:** коли я не знав, як реалізувати або пояснити окремі частини Java-програми, я звертався до чат-бота за поясненнями. Також я звертався до Codex для рев'ю коду, пошуку потенційних помилок і перевірки документації.

**Редакційний процес:** усі рекомендації чат-бота та Codex перевірялися мною самостійно. Результати перевірено шляхом читання коду, компіляції, запуску програми та виконання Maven-команд.

**Джерела:** офіційна [документація Java SE 21](https://docs.oracle.com/en/java/javase/21/docs/api/), [документація Maven](https://maven.apache.org/guides/), а також власний код і дані цього репозиторію. Зовнішній код без змін не копіювався.

**Ваша відповідальність:** підтверджую, що розумію структуру та логіку написаного застосунку, самостійно перевірив результати й несу відповідальність за достовірність цього звіту.

## Висновки

У лабораторній роботі № 1 створено Java-програму для читання, перевірки та аналізу CSV-даних про лікарські засоби. Програма демонструє роботу з classpath-ресурсами, обробку помилок і розрахунок статистики. Отримана структура та результати стали основою для подальшого структурованого рішення в лабораторній роботі № 2.
